<?xml version="1.0" encoding="utf-8"?>
<!-- Фон диалоговых окон со своей разметкой ("Новая станция", "Выход", информационные окна).
     Задаётся темой (android:windowBackground), а не в разметке каждого окна - тогда у всех окон
     одинаковые фон и скругление, и менять их нужно в одном месте -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/background" />
    <corners android:radius="24dp" />
</shape>