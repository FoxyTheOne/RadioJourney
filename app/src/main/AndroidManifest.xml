<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
    Разрешения для выхода в интернет:
     -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!--
    Разрешения для Foreground location:
     -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!-- 1.
    Разрешение для Foreground Service:
     -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- Media3 ExoPlayer: не даём Wi-Fi и процессору уснуть, пока играет радио (C.WAKE_MODE_NETWORK) -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission
        android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC"
        android:minSdkVersion="34" />
    <uses-permission
        android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK"
        android:minSdkVersion="34" />
    <!--
        Разрешение для Alert Window:
    -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <!--
        When targeting Android 13 or higher, posting a permission requires holding the `POST_NOTIFICATIONS` permission:
    -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_radio"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RadioJourney"
        android:usesCleartextTraffic="true"
        tools:targetApi="m"> <!-- Added android:usesCleartextTraffic="true" because of HttpDataSource$HttpDataSourceException: Unable to connect -->
        <service
            android:name=".utils.exoplayer.MusicService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback">
            <intent-filter>
                <!-- Media3: MediaLibraryService (MusicService) -->
                <action android:name="androidx.media3.session.MediaLibraryService" />
                <action android:name="android.media.browse.MediaBrowserService" />
            </intent-filter>
        </service>
        <activity
            android:name=".presentation.MainActivity"
            android:configChanges="orientation"
            android:exported="true"
            android:screenOrientation="portrait"><!-- Здесь android:exported="true" позволяет запускать Activity из лаунчера -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
         2. Декларируем Foreground Service:
          -->
        <service
            android:name=".utils.service.ProgressForegroundService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />
        <!--
        Google maps:
        Старые версии модуля карт в Google Play services используют Apache HTTP, которого нет в Android 9+ по умолчанию.
        Без этой строки на таких устройствах приложение падает с NoClassDefFoundError: org/apache/http/ProtocolVersion
         -->
        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />
    </application>

</manifest>